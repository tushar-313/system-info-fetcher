global:
  resolve_timeout: 5m

route:
  receiver: notifier
  group_by: ["alertname", "job"]
  group_wait: 10s
  group_interval: 2m
  repeat_interval: 4h

receivers:
  - name: notifier
    webhook_configs:
      - url: http://notifier:8080/alertmanager
        send_resolved: true
